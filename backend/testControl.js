/* File script test local use controllers and file testControllers.js
Add file testControllers.js request asynchronous for stimul the server express*/

//const ScriptTest = require("./src/controllers/testControllers");

// fake objects req/res

/*const fakeReq = { params: { id: 1 }, body: {} };
const fakeRes = {
  send: (data) => {
    console.log("📦 send:", data);
  },
  sendStatus: (code) => console.log("📡 status:", code),
  location: (url) => ({
    sendStatus: (code) => console.log("📍 location:", url, "status:", code),
  }),
};

(async () => {
  console.log("Test ici");
  await ScriptTest.browse(fakeReq, fakeRes);

  console.log("Test lu");
  await ScriptTest.read(fakeReq, fakeRes);

  process.exit();
})();
*/
